# AlaMGR configuration — copy to ~/.config/alamgr/env and edit
# ALAMGR_DEV_ROOT="$HOME/dev"
# ALAMGR_STRIP_SUFFIX=".example.local"
#
# Tmux settings applied when creating a new remote session (one command per line).
# Clear this variable to apply no settings.
# ALAMGR_REMOTE_TMUX_OPTS="set -g mouse on
# set-option -g set-clipboard external
# bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'tmux save-buffer -'
# bind-key -T copy-mode     MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'tmux save-buffer -'"
